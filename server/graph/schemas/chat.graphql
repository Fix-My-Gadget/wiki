# ===============================================
# CHAT
# ===============================================

extend type Query {
  chatAsk(
    question: String!
    draft: Boolean
  ): ChatResponse! @auth(requires: ["read:pages"])
}

extend type Mutation {
  chatAsk(
    question: String!
    draft: Boolean
  ): ChatResponse! @auth(requires: ["read:pages"])
}

# -----------------------------------------------
# TYPES
# -----------------------------------------------

type ChatResponse {
  answer: String!
  pageDraft: ChatPageDraft
}

type ChatPageDraft {
  title: String!
  content: String!
}

